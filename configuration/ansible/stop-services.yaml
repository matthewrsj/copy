---
# ansible-playbook -l <host_group> stop-services.yaml -K
- hosts: all
  tasks:
  - name: Start protostream
    service:
      name: protostream.service
      state: stopped
    become: yes
    become_method: sudo
    tags:
      - protostream
  - name: Start towercontroller
    service:
      name: towercontroller.service
      state: stopped
    become: yes
    become_method: sudo
    tags:
      - towercontroller
  - name: Start towerstreamsvc
    service:
      name: towerstreamsvc.service
      state: stopped
    become: yes
    become_method: sudo
    tags:
      - towerstreamsvc
  - name: Start canup
    service:
      name: canup.service
      state: stopped
    become: yes
    become_method: sudo
    tags:
      - canup
